<form method="post" action="{{ path('app_demande_emploi_delete', {'id': demande_emploi.getDemande_id()}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_emploi.getDemande_id()) }}">
    <button class="btn">Delete</button>
</form>