{# templates/entrepreneur/dashboard.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
<div class="container-fluid fade-in">
    <!-- Header -->
    <div class="header">
        <div>
            <h1>Tableau de Bord Entrepreneur</h1>
            <div class="page-subtitle">Bienvenue, {{ app.user.nom ?? 'Entrepreneur' }}. Gérez vos projets, vos équipes et vos tâches.</div>
        </div>
        <a href="{{ path('entrepreneur_nouveau_projet') }}" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Nouveau Projet
        </a>
    </div>
    
    <!-- Stats Grid -->
    <div class="stats-grid">
        <div class="stats-card">
            <i class="bi bi-folder2-open stats-icon" style="color: var(--color-primary);"></i>
            <h6>Total Projets</h6>
            <h3>{{ stats.total }}</h3>
        </div>
        <div class="stats-card">
            <i class="bi bi-clock-history stats-icon" style="color: var(--color-warning);"></i>
            <h6>En Attente</h6>
            <h3>{{ stats.en_attente }}</h3>
        </div>
        <div class="stats-card">
            <i class="bi bi-check-circle stats-icon" style="color: var(--color-success);"></i>
            <h6>Acceptés</h6>
            <h3>{{ stats.acceptes }}</h3>
        </div>
        <div class="stats-card">
            <i class="bi bi-trophy stats-icon" style="color: var(--color-accent);"></i>
            <h6>Terminés</h6>
            <h3>{{ stats.termines }}</h3>
        </div>
    </div>
    
    <!-- Search and Filters -->
    <div class="search-filters">
        <form method="get" class="row g-3 dynamic-filter">
            <div class="col-md-6">
                <div class="search-box">
                    <i class="bi bi-search"></i>
                    <input type="text" name="search" class="form-control" placeholder="Rechercher un projet..." value="{{ search }}">
                </div>
            </div>
            <div class="col-md-3">
                <select name="status" class="form-select" onchange="this.form.submit()">
                    <option value="">Tous les statuts</option>
                    <option value="En attente" {{ status == 'En attente' ? 'selected' : '' }}>En attente</option>
                    <option value="Accepté" {{ status == 'Accepté' ? 'selected' : '' }}>Acceptés</option>
                    <option value="Refusé" {{ status == 'Refusé' ? 'selected' : '' }}>Refusés</option>
                    <option value="En cours" {{ status == 'En cours' ? 'selected' : '' }}>En cours</option>
                    <option value="Terminé" {{ status == 'Terminé' ? 'selected' : '' }}>Terminés</option>
                </select>
            </div>
            <div class="col-md-3">
                <select name="sort" class="form-select" onchange="this.form.submit()">
                    <option value="date_soumission" {{ sort == 'date_soumission' ? 'selected' : '' }}>Date de soumission</option>
                    <option value="titre" {{ sort == 'titre' ? 'selected' : '' }}>Titre</option>
                    <option value="etat" {{ sort == 'etat' ? 'selected' : '' }}>Statut</option>
                </select>
            </div>
        </form>
    </div>
    
    <!-- Projects List -->
    <div class="card-modern">
        <div class="card-header-modern">
            <h3><i class="bi bi-folder2"></i> Mes Projets</h3>
            <span class="badge" style="background: rgba(255,255,255,0.2);">{{ projets|length }} projet(s)</span>
        </div>
        <div class="card-body-modern">
            {% if projets|length > 0 %}
                <div class="table-responsive">
                    <table class="custom-table">
                        <thead>
                            <tr>
                                <th>Projet</th>
                                <th>Date de soumission</th>
                                <th>Statut</th>
                                <th>Progression</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for projet in projets %}
                            <tr>
                                <td data-label="Projet">
                                    <div class="fw-bold">{{ projet.titre }}</div>
                                    <div class="small text-muted">{{ projet.description|slice(0, 60) }}...</div>
                                </td>
                                <td data-label="Date">{{ projet.dateSoumission|date('d/m/Y') }}</td>
                                <td data-label="Statut">
                                    <span class="status-badge 
                                        {% if projet.etat == 'En attente' %}status-pending
                                        {% elseif projet.etat == 'Accepté' %}status-accepted
                                        {% elseif projet.etat == 'Refusé' %}status-refused
                                        {% elseif projet.etat == 'En cours' %}status-in-progress
                                        {% else %}status-completed{% endif %}">
                                        {{ projet.etat }}
                                    </span>
                                </td>
                                <td data-label="Progression">
                                    {% if projet.etat == 'Accepté' or projet.etat == 'En cours' %}
                                        <div class="progress-container">
                                            <div class="progress-bar-wrapper">
                                                <div class="progress">
                                                    <div class="progress-bar" style="width: {{ projet.progression }}%"></div>
                                                </div>
                                            </div>
                                            <span class="progress-value">{{ projet.progression }}%</span>
                                        </div>
                                    {% else %}
                                        —
                                    {% endif %}
                                </td>
                                <td data-label="Actions">
                                    <div class="btn-group" role="group">
                                        {% if projet.etat == 'Accepté' or projet.etat == 'En cours' %}
                                            <a href="{{ path('entrepreneur_gestion_projet', {'id': projet.idProjet}) }}" 
                                               class="btn btn-sm btn-primary">
                                                <i class="bi bi-gear"></i> Gérer
                                            </a>
                                        {% endif %}
                                        <a href="{{ path('entrepreneur_projet_detail', {'id': projet.idProjet}) }}" 
                                           class="btn btn-sm btn-outline-primary">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% else %}
                <div class="text-center py-5">
                    <i class="bi bi-inbox display-1 text-muted"></i>
                    <p class="text-muted mt-3">Aucun projet trouvé</p>
                    <a href="{{ path('entrepreneur_nouveau_projet') }}" class="btn btn-primary mt-2">
                        <i class="bi bi-plus-circle"></i> Créer mon premier projet
                    </a>
                </div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}